# toxic-comment-multilabel-classification
Built a multi-label text classification system to detect toxic online comments across six categories. Compared TF-IDF + SVM, BiLSTM, and DistilBERT models, using F1-score and per-label threshold tuning. DistilBERT achieved the strongest overall performance.
